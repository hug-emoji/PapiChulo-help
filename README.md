Compile instructions for cmake:

- Step 1: generate makefile (folder "build" will be created)
```
cmake -B build
```
- Step 2: Build the executable 
```
cmake --build build
```
- The executable will now be in the folder "build". To run:
```
build/movies
```


